fname = input("Enter file name: ")
fh = open(fname)
lst = list() # We define an empty list, this list will store all words in the given file 

for line in fh: # First we convert each line of the giving text in to a list 
    romeo = line.split() # This variable will store the lists of wors generated by the funtion ".split" in the loop
        for word in romeo: # We stard the process to put everything in one
          if word not in lst: #This is the condition that will add all the lists in the empty list
            lst.append(words)
          else:
            continue
  print(sorted(lst))
    

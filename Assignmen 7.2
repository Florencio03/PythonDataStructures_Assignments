# Use the file name mbox-short.txt as the file name

fname = input("Enter file name: ")

fhname= open(fname)
count = 0
spam = 0

for line in fh:
    # Skip 'uninteresting lines' and convert the ones of interest in to boolean
    if not line.startswith("X-DSPAM-Confidence:"): #with this condition we start to add... 
        continue 
    spam += float(line[20:-1].strip())
    count = count + 1
    #print(line)
    
print("Average spam confidence:", (spam/count))
